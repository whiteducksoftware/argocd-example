---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-certificate
spec:
  secretName: ingress-certificate
  issuerRef: 
    kind: ClusterIssuer
    name: letsencrypt
  commonName: "*.demo.meisenzahl.org"
  dnsNames:
    - "*.demo.meisenzahl.org"